{
  "description": "Guards for a git working tree shared by more than one Claude Code session. Registers the session in <git-common-dir>/claude-sessions/ and, only when another live session shares this working tree, guards the shared index against a commit that would sweep up their staged work and the tree against a checkout/restore/reset that would overwrite their uncommitted changes.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks-handlers/cotenancy.cjs\"",
            "timeout": 15
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks-handlers/cotenancy.cjs\"",
            "timeout": 15
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks-handlers/commit-guard.cjs\"",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks-handlers/overwrite-guard.cjs\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
